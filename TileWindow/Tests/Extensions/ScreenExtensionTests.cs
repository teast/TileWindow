using TileWindow.Dto;
using Xunit;
using FluentAssertions;
using TileWindow.Handlers.I3wm.Nodes;
using System;

namespace TileWindow.Tests.Dto
{
    public class ScreenExtensionTests
    {
        /*
        [Fact]
        public void When_Merging_One_Rect_Then_Return_One_Rect()
        {
            var input = new RECTex(0, 0, 100, 100);

            var result = RECTex.Merge(input);

            result.Rect.Equals(input.Rect).Should().BeTrue();
            result.Rect2.Should().BeNull();
        }
        */
    }
}
